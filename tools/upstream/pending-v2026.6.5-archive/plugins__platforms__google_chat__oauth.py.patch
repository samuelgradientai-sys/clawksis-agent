--- a/plugins/platforms/google_chat/oauth.py
+++ b/plugins/platforms/google_chat/oauth.py
@@ -50,10 +50,8 @@
     ``${CLAWK_HOME}/google_chat_user_oauth_pending/<sanitized_email>.json``
 - Legacy pending state:
     ``${CLAWK_HOME}/google_chat_user_oauth_pending.json``
-- Shared OAuth client (one per host, anchored at the default Clawksis root so
-  every profile sees it; a profile-local copy under ``${CLAWK_HOME}`` wins
-  when present):
-    ``<default-root>/google_chat_user_client_secret.json`` (default ``~/.clawksis``)
+- OAuth client secret (profile-scoped — each profile registers its own):
+    ``${CLAWK_HOME}/google_chat_user_client_secret.json``
 """
 
 from __future__ import annotations
@@ -77,11 +75,7 @@
 # Use the project's CLAWK_HOME helper so the token follows the user's
 # profile (e.g. tests can override via CLAWK_HOME=/tmp/...).
 try:
-    from clawk_constants import (
-        display_clawk_home,
-        get_default_clawk_root,
-        get_clawk_home,
-    )
+    from clawk_constants import display_clawk_home, get_clawk_home
 except (ModuleNotFoundError, ImportError):
     # Fallback for environments where clawk_constants isn't importable
     # (mirrors the same fallback used by the google-workspace skill's
@@ -90,24 +84,6 @@
         val = os.environ.get("CLAWK_HOME", "").strip()
         return Path(val) if val else Path.home() / ".clawk"
 
-    def get_default_clawk_root() -> Path:
-        # Mirror clawk_constants.get_default_clawk_root(): resolve the
-        # profile root so host-wide files (the shared client secret) are
-        # found regardless of which profile is active.
-        native_home = Path.home() / ".clawk"
-        env_home = os.environ.get("CLAWK_HOME", "").strip()
-        if not env_home:
-            return native_home
-        env_path = Path(env_home)
-        try:
-            env_path.resolve().relative_to(native_home.resolve())
-            return native_home
-        except ValueError:
-            pass
-        if env_path.parent.name == "profiles":
-            return env_path.parent.parent
-        return env_path
-
     def display_clawk_home() -> str:
         home = get_clawk_home()
         try:
@@ -164,24 +140,7 @@
 
 
 def _client_secret_path() -> Path:
-    """Path to the shared OAuth client secret (one per host).
-
-    The client secret identifies the OAuth *app*, not a user or a profile,
-    so it is anchored at the default Clawksis root (``~/.clawksis`` — or the
-    Docker root) rather than the active profile's ``CLAWK_HOME``. That way
-    the one-time ``--client-secret`` host setup is visible to gateways
-    running under any named profile, exactly as the docs describe ("one
-    file per host is enough no matter how many users authorize later").
-
-    A profile-local secret (``$CLAWK_HOME/google_chat_user_client_secret.json``)
-    still takes precedence when present, for installs that seeded one under
-    the previous profile-scoped behavior or that deliberately run a separate
-    OAuth app per profile.
-    """
-    profile_local = _clawk_home() / "google_chat_user_client_secret.json"
-    if profile_local.exists():
-        return profile_local
-    return get_default_clawk_root() / "google_chat_user_client_secret.json"
+    return _clawk_home() / "google_chat_user_client_secret.json"
 
 
 def _pending_auth_path(email: Optional[str] = None) -> Path:
